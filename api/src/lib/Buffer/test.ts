// The test buffer stores views in redis then flushes them to clickhouse on occassion
import { describe, it, expect } from '@jest/globals';

describe('Buffer', () => {
    it('should have a test', () => {
        expect(true).toBe(true);
    });
});
