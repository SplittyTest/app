export { addTestToQueue } from './addTestToQueue';
export { getTestVariationById } from './getTestVariationById';
export { removeTestFromQueue } from './removeTestFromQueue';
export { selectTestVariation } from './selectTestVariation';
export { selectTest } from './selectTest';
export * as standardError from './standardError';

/*

logEvent(event_name: string, value: number) {
    // Get session data from the cookie
    // Save an event with the current session data
}

logEventOnce(event_name: string, value: number) {
    // Get session data from the cookie
    // Only save an event if one doesn't already exist for this session
}
*/
