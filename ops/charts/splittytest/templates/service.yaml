apiVersion: v1
kind: Service
metadata:
  name: {{ include "splittytest.fullname" . }}
  labels:
    {{- include "splittytest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "splittytest.selectorLabels" . | nindent 4 }}
