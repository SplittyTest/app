apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "splittytest.fullname" . }}-test-connection"
  labels:
    {{- include "splittytest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "splittytest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
